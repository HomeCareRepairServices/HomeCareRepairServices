import type { Metadata } from "next"
import { siteConfig } from "@/lib/site"
import { services } from "@/lib/data/services"
import { areas } from "@/lib/data/areas"
import { Breadcrumb, ContactActions, ContactCard, SupportHero, SupportShell } from "@/components/support-page"
export const metadata: Metadata = { title: `Contact ${siteConfig.name} | Rishikesh, Haridwar & Dehradun`, description: "Contact Home Care Repair Services for home appliance repair, installation and maintenance across Rishikesh, Haridwar, Dehradun and nearby areas." }
export default function ContactPage() { return <SupportShell><Breadcrumb current="Contact" /><SupportHero eyebrow="Get in Touch" title={`Contact ${siteConfig.name}`} description="Tell us what needs attention and our team can help you choose the right doorstep service." /><section className="mx-auto grid max-w-7xl gap-8 px-4 py-16 sm:px-6 lg:grid-cols-[.8fr_1.2fr] lg:px-8"><ContactCard /><form className="rounded-2xl border border-border bg-card p-6"><h2 className="text-2xl font-semibold text-foreground">Send a service request</h2><p className="mt-2 text-muted-foreground">Share your details and preferred service. We will use this information to respond to your request.</p><div className="mt-6 grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-medium">Name<input name="name" required className="rounded-lg border border-border bg-background px-3 py-3" /></label><label className="grid gap-2 text-sm font-medium">Phone Number<input name="phone" type="tel" required className="rounded-lg border border-border bg-background px-3 py-3" /></label><label className="grid gap-2 text-sm font-medium">Service Required<select name="service" className="rounded-lg border border-border bg-background px-3 py-3">{services.map((s) => <option key={s.slug}>{s.name}</option>)}</select></label><label className="grid gap-2 text-sm font-medium">Location<select name="location" className="rounded-lg border border-border bg-background px-3 py-3">{areas.map((a) => <option key={a.slug}>{a.name}</option>)}</select></label></div><label className="mt-4 grid gap-2 text-sm font-medium">Message<textarea name="message" rows={4} className="rounded-lg border border-border bg-background px-3 py-3" /></label><button type="submit" className="mt-5 rounded-lg bg-primary px-5 py-3 font-semibold text-primary-foreground">Send Request</button></form></section><section className="mx-auto max-w-7xl px-4 pb-16 sm:px-6 lg:px-8"><ContactActions /></section></SupportShell> }
